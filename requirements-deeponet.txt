# Dependencies for deeponet_smoother.py
#
# CPU-only PyTorch is enough: nothing here needs a GPU, and the level sizes this
# is aimed at (n_dof = 1024 .. 4096) train in minutes on a laptop CPU.
#
#   python -m pip install -r requirements-deeponet.txt
#   python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# The second line is separate on purpose: the PyTorch CPU index does not carry
# numpy/scipy/matplotlib, so mixing the two in one command fails to resolve.
# Verified with numpy 2.2.6, scipy 1.15.3, matplotlib 3.10.9, torch 2.14.0+cpu
# on CPython 3.10 (Windows) and CPython 3.12 (Linux).

numpy>=1.24
scipy>=1.10
matplotlib>=3.6
torch>=2.0
pyamg>=5.3,<6
